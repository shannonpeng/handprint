<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard | Handprint</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <link rel="shortcut icon" href="images/favicon.png" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="/js/main.js"></script>
  </head>
  <body>

  	<nav>

  		<!-- Logo -->
  		<div class="logo">
  			<img src="images/favicon.png">
  		</div>

  		<!-- Links -->
  		<div class="links">
	  		<div><a href="/profile">profile</a></div>
	  		<div><a href="/help">help</a></div>
		  	<a href="/dashboard"><div class="account">
  				<div><h3>{{ user.name }}</h3></div>
  				<div class="profile-picture" style="background-image:url('{{ user.profile_pic_url}}')">
  				</div>
			</div></a>
  		</div>	

  	</nav>

  	<div class="container">

  		<div id="dashboard">
  		
	  		<!--create challenge for organizations only
	  		<div class="create-challenge">
	  			<div class="add-challenge-icon" style="background-image:url('images/plus-icon.png')"></div>
	  			<div class="challenge-input-box">
	  				<form name="addchallenge" method="post" action="/addchallenge">
	  				    <ul>
	  				    <li><input type="text" placeholder="Create challenge" name="create-challenge"></li>
	  				    </ul>
	  				</form>
	  			</div>
	  		</div>
	  		-->
	  		
	  		<!-- TODO: add friends' recent history to profile -->

	  		<div class="challenges">
	  			{{#each challenges}}
	  			<div class="challenge">
	  				<h3>{{ title }}</h3>
	  				<p>{{ description }}</p>
	  				<p>Start Date: {{ start_date }}</p>
	  				<p>End Date: {{ end_date }}</p>
	  				<p>Location: {{ location }}</p>
	  				<p>Points: {{ points }}</p>
	  			</div>
	  			{{/each}}
	  		</div>

	  		<div class="friends">
	  			{{#each friend}}
	  			<div class="friend">
	  				<h3>{{ name }}</h3>
	  				<img src="{{ profile_pic_url }}">
	  				<p>{{ level }}</p>
	  				<p>{{ points }}</p>
	  			</div>
	  			{{/each}}
		  		</div>
		  	</div>

		</div>
  		
  	</div>

  </body>
</html>
