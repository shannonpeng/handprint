<!DOCTYPE html>
<html>
  <head>
    <title>{{ org.name }} | Handprint</title>
    <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro" rel="stylesheet">
    <meta content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0' name='viewport' />
    <link rel="stylesheet" href="/stylesheets/base.css" />
    <link rel="stylesheet" href="/stylesheets/profile.css" />
    <link rel="shortcut icon" href="/favicon.png" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script src="/js/main.js"></script>
  </head>
  <body>

  	{{>navbar}}

  	<div class="container">

  		<div id="profile">
  		
	  		<div class="profile-header" style="background: linear-gradient(45deg, rgba(61, 158, 223, 0.8), rgba(16, 102, 213, 0.8)), url('{{ org.cover_pic_url }}');background-size: cover;">
	  			<div class="container">
		  			<div class="profile-picture" style="background-image:url('{{ org.profile_pic_url}}')"></div>
		  			<div class="info">
		  				<h1 class="heavy">{{ org.name }}</h1>
		  				<h3 class="org-location">{{ org.location }}</h3>
			  			<p class="org-description">{{ org.bio }}</p>
		  			</div>
		  			{{#if account }}
			  			{{#if notSelf}}
			  			{{else}}
			  				<a href="/edit-profile"><div class="button">manage account</div></a>
			  		{{/if}}
			  		{{/if}}
		  		</div>
	  		</div>

	  		<div class="content">

			  	<div class="challenges">
		  			<h2>Challenges</h2>
		  			{{#each challenges}}
		  			<div class="challenge">
		  				<div class="challenge-points">
		  					<h1>{{ points }}</h1>
		  				</div>
		  				<div class="challenge-info">
		  					<b><p class="challenge-title">{{ title }}</p></b>
		  					<p>{{ description }}</p>
		  					<p class="challenge-location">{{ location_name }}</p>
		  					<p class="challenge-date">{{ start_date }} &mdash; {{ end_date }}</p>
		  					<div class="challenge-tags">
		  						{{#each category_tags}}
		  						<div><p># {{ this }}</p></div>
		  						{{/each}}
		  					</div>
		  					<div class="challenge-users">
		  						<ul>
		  						{{#each users }}
		  						<li><a href="/profile/{{username}}"><div style="background:linear-gradient(0deg, rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.1)), url('{{ profile_pic_url }}');background-size:cover">
		  						</div></a></li>
		  						{{/each}}
		  						</ul>
		  					</div>
		  				</div>
		  			</div>
		  			{{else}}
		  			<h3>{{ org.name }} has not yet posted any challenges.</h3>
		  			{{/each}}
		  		</div>

	  		</div>
		</div>
  		
  	</div>

  </body>
</html>
